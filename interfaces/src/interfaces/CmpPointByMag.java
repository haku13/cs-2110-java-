/*package interfaces;

import java.util.Comparator;

public class CmpPointByMag implements Comparator<Point> {

	public int compare(Point p1, Point p2){
		double dp1 = Math.sqrt(p1.getX() * p1.getX() + p1.getY() * p1.getY());
		double dp2 = Math.sqrt(p2.getX() * p2.getX() + p2.getY() * p2.getY());
		
		if(dp1 > dp2){
			return 1;
		}else if(dp1 < dp2){
			return -1;
		}else{
			return 0;
		}
	}
}
*/